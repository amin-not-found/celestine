function main(): int = {
    let zero;
    let temp = zero = 48;
    let line_break = 10;

    putchar 1 + zero;
    putchar line_break;

    // (zero - 1 + 3) = '2'
    temp = (temp + -1)*2/2 + (2 - -1);
    putchar temp;
    putchar line_break;
    return 0;
}